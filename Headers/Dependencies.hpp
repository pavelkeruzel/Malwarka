#ifndef HEADER_FILE_H
#define HEADER_FILE_H   // Ensures the header file is only included once during compilation

#include <Windows.h>    // Includes necessary Windows headers
#include <iostream>     // Includes standard input-output stream library
#include <string>       // Includes string library for std::string
#include <map>          // Includes map library for std::map
#include <stdexcept>    // Includes exception handling library for std::runtime_error, std::logic_error, etc.
#include <vector>       // Includes vector library for std::vector
#include <unordered_map>// Includes unordered_map library for std::unordered_map
#include <algorithm>    // Includes algorithm library for std::sort, std::find, etc.
#include <tuple>        // Includes tuple library for std::tuple
#include <functional>   // Includes functional library for std::function, std::bind, etc.
#include <winnt.h>      // Includes definitions for MEMORY_INFORMATION_CLASS and other Windows NT definitions

#endif // HEADER_FILE_H
